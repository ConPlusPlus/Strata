// Generated by stratac. Do not edit.
#define STRATA_PROGRAM 1
#include <stdint.h>
#include <stdbool.h>
#include <stdio.h>
#include <arena.h>
#include <smath.h>
#include <sstr.h>
#include <sio.h>
#include <sarr.h>
#include <sprelude.h>

typedef struct Point Point;
struct Point {
    int64_t x;
    int64_t y;
};

int64_t sum(Point* p);

int64_t sum(Point* p) {
    return (p->x + p->y);
}

int main(int strata_argc_, char** strata_argv_) {
    strata_set_args(strata_argc_, strata_argv_);
    Arena world = arena_make();
    Point* p = (Point*)arena_alloc(&world, sizeof(Point));
    p->x = 3;
    p->y = 4;
    printf("%lld\n", (long long)(sum(p)));
    printf("%lld\n", (long long)(p->x));
    {
        Arena frame = arena_make();
        Point* q = (Point*)arena_alloc(&frame, sizeof(Point));
        q->x = 10;
        q->y = 32;
        printf("%lld\n", (long long)(sum(q)));
        arena_free(&frame);
    }
    return 0;
}
