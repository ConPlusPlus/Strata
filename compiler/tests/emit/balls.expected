// Generated by stratac. Do not edit.
#define STRATA_PROGRAM 1
#include <stdint.h>
#include <stdbool.h>
#include <stdio.h>
#include <arena.h>
#include <smath.h>
#include <sstr.h>
#include <sio.h>
#include <sarr.h>
#include <sprelude.h>
#include <raylib.h>

typedef struct Ball Ball;
struct Ball {
    vec2 pos;
    vec2 vel;
};


int main(int strata_argc_, char** strata_argv_) {
    strata_set_args(strata_argc_, strata_argv_);
    InitWindow(800, 450, "Strata - bouncing balls");
    SetTargetFPS(60);
    Array balls = arr_make(sizeof(Ball));
    for (int64_t i = 0; i < 60; i++) {
        Ball b;
        b.pos = (vec2){GetRandomValue(40, 760), GetRandomValue(40, 410)};
        b.vel = (vec2){GetRandomValue((-200), 200), GetRandomValue((-200), 200)};
        ({ Ball _e = b; arr_push(&(balls), &_e); });
    }
    while ((!WindowShouldClose())) {
        float dt = GetFrameTime();
        for (int64_t i = 0; i < balls.len; i++) {
            ((Ball*)(balls).data)[i].pos = vec2_add(((Ball*)(balls).data)[i].pos, vec2_scale(((Ball*)(balls).data)[i].vel, dt));
            if ((((Ball*)(balls).data)[i].pos.x < 10)) {
                ((Ball*)(balls).data)[i].vel.x = (0 - ((Ball*)(balls).data)[i].vel.x);
            }
            if ((((Ball*)(balls).data)[i].pos.x > 790)) {
                ((Ball*)(balls).data)[i].vel.x = (0 - ((Ball*)(balls).data)[i].vel.x);
            }
            if ((((Ball*)(balls).data)[i].pos.y < 10)) {
                ((Ball*)(balls).data)[i].vel.y = (0 - ((Ball*)(balls).data)[i].vel.y);
            }
            if ((((Ball*)(balls).data)[i].pos.y > 440)) {
                ((Ball*)(balls).data)[i].vel.y = (0 - ((Ball*)(balls).data)[i].vel.y);
            }
        }
        BeginDrawing();
        ClearBackground(RAYWHITE);
        { Array _arr = balls;
            for (int64_t _i = 0; _i < _arr.len; _i++) {
                Ball b = ((Ball*)_arr.data)[_i];
                DrawCircle(b.pos.x, b.pos.y, 8.0, MAROON);
            }
        }
        DrawText("bouncing balls, written in Strata", 10, 10, 20, DARKGRAY);
        EndDrawing();
    }
    CloseWindow();
    return 0;
}
