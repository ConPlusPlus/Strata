// Generated by stratac. Do not edit.
#define STRATA_PROGRAM 1
#include <stdint.h>
#include <stdbool.h>
#include <stdio.h>
#include <arena.h>
#include <smath.h>
#include <sstr.h>
#include <sio.h>
#include <sarr.h>
#include <sprelude.h>

typedef struct Entity Entity;
struct Entity {
    vec3 pos;
    vec3 vel;
    int64_t hp;
};

vec3 update(Entity* e, float dt);

vec3 update(Entity* e, float dt) {
    e->pos = vec3_add(e->pos, vec3_scale(e->vel, dt));
    return e->pos;
}

int main(int strata_argc_, char** strata_argv_) {
    strata_set_args(strata_argc_, strata_argv_);
    printf("%s\n", "hello, strata");
    Arena world = arena_make();
    Entity* e = (Entity*)arena_alloc(&world, sizeof(Entity));
    e->hp = 100;
    e->vel = (vec3){1, 0, 0};
    for (int64_t i = 0; i < 60; i++) {
        update(e, 0.016);
    }
    printf("%g\n", (double)(vec3_length(e->pos)));
    return 0;
}
