// Generated by stratac. Do not edit.
#define STRATA_PROGRAM 1
#include <stdint.h>
#include <stdbool.h>
#include <stdio.h>
#include <arena.h>
#include <smath.h>
#include <sstr.h>
#include <sio.h>
#include <sarr.h>
#include <sprelude.h>

typedef struct Node Node;
struct Node {
    int64_t val;
    Node* next;
};


int main(int strata_argc_, char** strata_argv_) {
    strata_set_args(strata_argc_, strata_argv_);
    Node* head = ({ Node _v = (Node){3, 0}; Node* _p = (Node*)arena_alloc(strata_heap(), sizeof(Node)); *_p = _v; _p; });
    head = ({ Node _v = (Node){2, head}; Node* _p = (Node*)arena_alloc(strata_heap(), sizeof(Node)); *_p = _v; _p; });
    head = ({ Node _v = (Node){1, head}; Node* _p = (Node*)arena_alloc(strata_heap(), sizeof(Node)); *_p = _v; _p; });
    int64_t sum = 0;
    Node* cur = head;
    while ((cur != 0)) {
        sum = (sum + cur->val);
        cur = cur->next;
    }
    printf("%lld\n", (long long)(sum));
    printf("%lld\n", (long long)(head->next->next->val));
    return 0;
}
