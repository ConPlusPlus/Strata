// Generated by stratac. Do not edit.
#define STRATA_PROGRAM 1
#include <stdint.h>
#include <stdbool.h>
#include <stdio.h>
#include <arena.h>
#include <smath.h>
#include <sstr.h>
#include <sio.h>
#include <sarr.h>
#include <sprelude.h>

typedef enum { Square, Wide, Tall } Kind;
typedef enum { Hidden } Secret;
typedef struct Rect Rect;
typedef struct Cache Cache;
struct Rect {
    int64_t w;
    int64_t h;
};
struct Cache {
    int64_t hits;
};

int64_t mods_shapes__helper(int64_t x);
int64_t area(Rect r);
Kind kind_of(Rect r);
const char* mods_words__helper(const char* s);
const char* tag(const char* s);
int64_t modules2__helper(int64_t x);

int64_t mods_shapes__helper(int64_t x) {
    return (x * 10);
}

int64_t area(Rect r) {
    return ((mods_shapes__helper(r.w) * r.h) / 10);
}

Kind kind_of(Rect r) {
    if ((r.w == r.h)) {
        return Square;
    }
    if ((r.w > r.h)) {
        return Wide;
    }
    return Tall;
}

const char* mods_words__helper(const char* s) {
    return str_concat(str_concat("<", s), ">");
}

const char* tag(const char* s) {
    return mods_words__helper(s);
}

int64_t modules2__helper(int64_t x) {
    return (x + 1);
}

int main(int strata_argc_, char** strata_argv_) {
    strata_set_args(strata_argc_, strata_argv_);
    Rect r = (Rect){4, 3};
    printf("%lld\n", (long long)(area(r)));
    printf("%s\n", tag("hi"));
    printf("%lld\n", (long long)(modules2__helper(41)));
    printf("%lld\n", (long long)((kind_of(r) == Wide)));
    return 0;
}
